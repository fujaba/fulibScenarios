# --------------- General Settings ---------------

language: java
jdk: oraclejdk8
services:
  - docker

# --------------- Rancher Deployment via webapp branch ---------------

before_install:
  - sudo apt-get update
  - sudo apt-get install -y jq

deploy:
  provider: script
  script: bash travis-deploy.sh
  on:
    branch: webapp

# --------------- Gradle Cache ---------------

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
